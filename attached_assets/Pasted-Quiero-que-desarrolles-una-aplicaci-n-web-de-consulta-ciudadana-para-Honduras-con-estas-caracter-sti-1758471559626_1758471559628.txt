Quiero que desarrolles una aplicación web de consulta ciudadana para Honduras con estas características:

🖥️ General

Usa Node.js con Express para el backend.

Usa React con Bootstrap 5 para el frontend.

La base de datos debe ser PostgreSQL (o SQLite si es más fácil para prototipo).

Todo debe funcionar en Replit free tier, sin costos adicionales.

El tiempo estimado de desarrollo es 1 semana, así que organiza el código por módulos claros.

🎨 Diseño (UI/UX)

Colores principales:

Celeste Honduras #1bd1e8

Blanco #ffffff

Gris claro #f5f5f5

Usa Bootstrap Icons en botones y formularios.

El diseño debe ser atractivo y fácil de usar.

📝 Funcionalidades principales
1. Formulario ciudadano

Tipo de persona: Natural, Jurídica o Anónimo.

Si es Natural → Primer nombre, Apellido, Identidad, Correo.

Si es Jurídica → Nombre de empresa, RTN, Representante legal, Correo/Teléfono.

Si es Anónimo → no solicita datos personales.

Datos de contacto opcionales: celular, teléfono fijo, correo (el ciudadano decide si los brinda).

Ubicación: selección jerárquica

Departamento → Municipio → Rural/Urbano → Colonia/Aldea.

Todo debe guardar geocódigo oficial.

Sectores:

Buscador inteligente (el ciudadano escribe y el sistema sugiere).

Puede seleccionar varios sectores a la vez.

Puede eliminar sectores seleccionados.

Campo de mensaje libre (consulta popular).

2. Roles

Ciudadano → llena el formulario.

Administrador/Planificador →

Ver tabla de datos recibidos.

Filtrar por fecha, sector, ubicación.

Exportar datos (CSV, Excel, PDF).

Ver dashboards con gráficas (Chart.js).

Super Administrador →

Solo crea y gestiona usuarios administradores/planificadores.

3. Dashboards

Gráficas con Chart.js o Recharts:

Número de consultas por sector.

Consultas por departamento/municipio (mapa de calor si es posible).

Evolución de consultas por fecha.

⚡ Requisitos técnicos

Usa JWT para la autenticación.

Organiza el proyecto en carpetas:

/backend (Express + DB)

/frontend (React + Bootstrap)

/models (tablas)

/routes (API REST)

/components (React).

Documenta el código con comentarios claros.

El sistema debe escalar para millones de registros (indexar por fecha y geocódigo).

👉 Genera primero la estructura del proyecto completa (carpetas, archivos y dependencias instaladas) y un formulario inicial con el diseño en celeste/blanco/gris, para luego avanzar módulo por módulo.